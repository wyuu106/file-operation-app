# Project Instruction

## 1. 概要

このプロジェクトは、バックオフィス担当者向けの
ローカルデスクトップファイル整理アプリケーションである。

主な目的は、日常的に発生する定型的なファイル名変更作業を
テンプレート化し、ユーザーの手動操作を減らすことである。

対象ユーザーはエンジニアではないため、
CLIやターミナル操作をユーザーに要求してはならない。

アプリケーションはローカルPC上で完結し、
外部サーバへのファイル送信や保存は行わない。

詳細な仕様は以下を参照すること。

- `docs/requirements.md`

---

## 2. 技術スタック

### フロントエンド

- React
- JavaScript
- Vite

### デスクトップアプリケーション

- Tauri

### バックエンド

- Rust
- Tauri Commands

### データベース

- SQLite

外部のWeb APIやクラウドサービスはMVPでは使用しない。

---

## 3. アーキテクチャ

基本構成は以下とする。

React
↓
Tauri invoke
↓
Rust
├── File System
└── SQLite

ReactはUIとユーザー操作を担当する。

Rustは以下を担当する。

- ファイルシステム操作
- ファイル名変更
- リネームプレビュー生成
- Undo処理
- SQLiteへのアクセス
- テンプレート管理

ReactからOSのファイルシステムを直接操作しない。

---

## 4. 開発規約

実装前に必ず関連コードと
`docs/requirements.md` を確認すること。

既存の設計を確認せずに新しい構造を導入しないこと。

変更は可能な限り小さくする。

依頼されていない大規模なリファクタリングを行わない。

コードは、1行50文字以内に収まるようにし、適度に改行する。

MVPの範囲を超える機能を勝手に追加しない。

仕様が不明確で、実装によってプロダクトの挙動が大きく変わる場合は、
推測で実装せず確認すること。

---

## 5. UXの規約

最重要目標は、

「バックオフィス担当者の手動操作を減らすこと」

である。

UIを設計するときは常に、

- この入力は本当に必要か
- 自動取得できないか
- 前回の設定を再利用できないか
- 操作回数を減らせないか

を検討すること。

一方で、自動化によってユーザーが意図しない
ファイル変更が発生してはならない。

そのため、

自動化より安全性を優先する。

---

## 6. File Operation Safety

ファイル名変更は破壊的操作になり得るため、
特に慎重に実装すること。

リネーム実行前に必ずプレビューを生成する。

以下を事前に検証する。

- 同名ファイルの存在
- 重複する変更後ファイル名
- OSで使用できないファイル名
- 対象ファイルの存在
- 対象フォルダへのアクセス可否
- 変更前と変更後が同一ではないか

### 上書き禁止

本アプリケーションは既存ファイルを上書きしてはならない。

事前の存在確認のみを上書き防止手段として使用してはならない。

存在確認後からリネーム実行までの間に
別プロセスによってファイルが作成される可能性を考慮する。

macOSおよびWindowsをMVPの対象OSとする。

リネーム処理では対象OSに応じて、
既存の移動先ファイルを置き換えない方法を使用する。

OS固有処理が必要な場合は、
上位のリネームロジックから分離して実装する。

上書き防止の具体的な実装方法は、
対象OSおよび使用するRust APIの仕様を確認した上で決定する。

### 部分的な処理エラーとロールバック

一括リネームは可能な限りAll-or-Nothingで実行する。

実行途中で失敗した場合、
それまでに成功したリネームを逆順にロールバックする。

ロールバック自体が失敗する可能性も考慮し、
各ファイルの実際の処理結果を追跡できるようにする。

既存ファイルを上書きすることで
ロールバックを成立させてはならない。

---

## 7. Undo

リネーム実行時には変更履歴を保存する。

最低限、

- 元のパス
- 変更後のパス
- 実行日時

を記録する。

Undoでは保存された履歴を利用して元のファイル名へ戻す。

Undoによって既存ファイルを上書きしてはならない。

---

## 8. データベース

SQLiteには主に以下を保存する。

- rename templates
- rename history

実際のユーザーファイルをSQLiteに保存してはならない。

ファイルの内容を外部へ送信してはならない。

---

## 9. Skills

作業内容に応じて以下を参照すること。

React UI:
`.agents/skills/frontend-dev/SKILL.md`

Tauri:
`.agents/skills/tauri-dev/SKILL.md`

Rustによるファイル操作:
`.agents/skills/rust-file-operations/SKILL.md`

SQLite:
`.agents/skills/database-dev/SKILL.md`

---

## 10. 検証

変更後は関連するテスト・ビルド・静的解析を実行する。

少なくとも、

- JavaScriptの型エラー
- Rustのコンパイルエラー
- Tauri CommandとReact側の引数不一致

がないことを確認する。

ファイル操作を変更した場合は、
通常ケースだけでなく異常ケースも確認する。

テストのためにユーザーの実ファイルを削除・上書きしない。

---

## 11. その他

### 判断に迷った場合

以下の優先順位で判断する。

1. ユーザーからの明示的な指示
2. `docs/requirements.md`
3. その他のプロジェクトドキュメント
4. 既存テスト
5. 既存コード
6. 買う技術スタックの一般的な慣習
7. 最も単純で影響範囲の小さい実装

以下に該当する場合のみ作業を停止して確認すること。

- データ損失の可能性がある
- セキュリティ上の重大な判断が必要
- 大規模なアーキテクチャ変更が必要
- 既存仕様を破壊する可能性がある
- 要件だけではユーザーの意図を合理的に判断できない

### Git

以下は禁止。

- `git reset --hard`
- force push
- ユーザーの未コミット変更の破棄
- 関係ないファイルの変更
- secretsのcommit

明示的に指示されない限りcommitは行わないこと。
